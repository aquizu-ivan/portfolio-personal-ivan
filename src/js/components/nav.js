// TODO: Implementar la navegación principal (links, scroll suave, estados activos).
export function createNav() {
  const nav = document.createElement('nav')
  nav.className = 'nav'
  nav.textContent = 'Navegación pendiente de implementar'
  return nav
}
